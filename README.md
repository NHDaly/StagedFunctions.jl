# StagedFunctions

A module containing our explorations around unlocking `@generated` functions to not need a
frozen world age by adding backedges to them.

# NOTE: Very experimental
- This Package requires Julia >= `v1.4.0-DEV.249` (after 2019-10-03).
